"""Schema version 2 — extended transaction models.

New versions inherit from the previous version and only add/override
fields that changed.  Never modify v1 models after data has landed
in Bronze.
"""
